'use strict';

module.exports = {
    reject: [
        // Todo: Try again after some time as caused number of tests to fail,
        //        whether in Node 18 or 20 (checked 24.1.0)
        'jsdom'
    ]
};
